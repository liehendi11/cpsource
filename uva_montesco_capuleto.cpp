#include <stdio.h>
#include <stdlib.h>
#include <set>

using namespace std;

set<int> enemies[202];
vector<int> friends;
vector<int> friendCounts;

void merge

int main()
{
	int tc;
	scanf("%d", &tc);

	for (int z=1; z<=tc; z++)
	{
		int n;
		scanf("%d", &n);

		for (int i=0; i<n; i++)
		{

		}
	}
	return 0;
}